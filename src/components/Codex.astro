---
import ChapterHead from "./ChapterHead.astro";
import { getDict, type Lang } from "../i18n";

// CODEX — the daily ledger, plus the two features people actually ask about:
// shared holiday pots (who paid, who owes whom) and subscriptions.

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = getDict(lang).codex;
const maxPaid = Math.max(...t.members.map((m) => Number(m.amount.replace("'", ""))));
---

<section id="codex" class="chapter" data-observe>
  <div class="wrap">
    <ChapterHead latin={t.latin} translation={t.translation} headline={t.headline} />

    <p class="intro" data-reveal style="--i: 2">{t.p1}</p>

    <div class="cards">
      <!-- one ledger: incomes + expenses, recurring marked ⟳ -->
      <div class="card veil" data-reveal style="--i: 3" role="img" aria-label={t.ledgerAria}>
        <header class="card-head">
          <p class="caps card-title">{t.month}</p>
          <p class="num verdict"><span class="muted">{t.budgetWord}</span> CHF 3'416 / 4'200</p>
        </header>
        <div class="budget-bar" aria-hidden="true"><div class="fill"></div></div>
        <ul class="rows">
          {
            t.entries.map((e, i) => (
              <li class="row" data-reveal style={`--i: ${i + 4}`}>
                <span class="payee">
                  <span class="payee-line">
                    {e.payee}
                    {e.rec && <span class="rec" aria-hidden="true">⟳</span>}
                  </span>
                  <small class="muted">{e.cat}</small>
                </span>
                <span class={`amount num ${e.kind}`}>{e.amount}</span>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- holiday pot with group split -->
      <div class="card veil" data-reveal style="--i: 4">
        <header class="card-head">
          <p class="caps card-title">{t.holidayTitle}</p>
          <p class="num verdict muted">{t.holidayTotal}</p>
        </header>
        <ul class="members num">
          {
            t.members.map((m) => (
              <li>
                <span class="mname">{m.name}</span>
                <span class="mbar" aria-hidden="true">
                  <span
                    class="mfill"
                    style={`width: ${Math.round((Number(m.amount.replace("'", "")) / maxPaid) * 100)}%`}
                  />
                </span>
                <span class="mamount">CHF {m.amount}</span>
              </li>
            ))
          }
        </ul>
        <p class="caps settle-label">{t.settleLabel}</p>
        <ul class="settles num">
          {t.settles.map((s) => <li>{s}</li>)}
        </ul>
      </div>
    </div>

    <!-- account connections: today, in the works, planned -->
    <div class="sources" data-reveal style="--i: 6">
      <span class="caps sources-label">{t.sourcesLabel}</span>
      {
        t.sources.map((s) => (
          <span class="source-chip">
            <strong>{s.name}</strong>
            <em class="muted">{s.status}</em>
          </span>
        ))
      }
    </div>

    <p class="caps foot muted" data-reveal style="--i: 7">{t.foot}</p>
  </div>
</section>

<style>
  .intro {
    max-width: 46em;
    margin-bottom: clamp(1.6rem, 3vh, 2.4rem);
    font-size: 1.15rem;
  }

  .cards {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: clamp(1.2rem, 2.4vw, 2rem);
    align-items: stretch;
  }

  .card {
    padding: 1.4rem 1.5rem 1.2rem;
    display: flex;
    flex-direction: column;
  }

  .payee-line {
    display: inline-flex;
    align-items: baseline;
    gap: 0.4rem;
  }

  /* the recurring marker */
  .rec {
    color: var(--gold);
    font-size: 0.85em;
  }

  .card-head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 0.8rem;
  }

  .card-title {
    color: var(--burgundy);
    font-size: 0.76rem;
  }

  .verdict {
    font-size: 0.98rem;
    font-weight: 600;
  }

  .budget-bar {
    height: 4px;
    background: var(--stone);
    border-radius: 2px;
    margin: 0.7rem 0 0.4rem;
    overflow: hidden;
  }

  .fill {
    height: 100%;
    width: 81%;
    background: var(--gold);
    transform-origin: left;
  }

  html.js [data-observe] .fill {
    transform: scaleX(0);
    transition: transform 1.1s var(--ease) 0.5s;
  }

  html.js [data-observe].is-inview .fill {
    transform: scaleX(1);
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .row {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    padding: 0.55rem 0.1rem;
    border-bottom: 1px solid rgba(216, 200, 174, 0.6);
  }

  .row:last-child {
    border-bottom: none;
  }

  .payee {
    display: grid;
    line-height: 1.3;
    font-size: 1.04rem;
  }

  .payee small {
    font-size: 0.88rem;
  }

  .amount {
    margin-left: auto;
    font-weight: 600;
    font-size: 1.04rem;
  }

  .amount.in {
    color: var(--sage);
  }

  /* holiday split */
  .members {
    margin-top: 0.8rem;
    display: grid;
    gap: 0.5rem;
  }

  .members li {
    display: grid;
    grid-template-columns: 3.4rem 1fr auto;
    align-items: center;
    gap: 0.7rem;
    font-size: 1rem;
  }

  .mname {
    font-weight: 500;
  }

  .mbar {
    height: 5px;
    background: var(--stone);
    border-radius: 3px;
    overflow: hidden;
  }

  .mfill {
    display: block;
    height: 100%;
    background: var(--gold);
  }

  .mamount {
    font-weight: 600;
  }

  .settle-label {
    font-size: 0.68rem;
    color: var(--taupe);
    margin-top: 0.9rem;
    padding-top: 0.7rem;
    border-top: 1px solid rgba(216, 200, 174, 0.6);
  }

  .settles {
    margin-top: 0.35rem;
    display: grid;
    gap: 0.25rem;
  }

  .settles li {
    font-size: 1.02rem;
    color: var(--sage);
    font-weight: 600;
  }

  .sources {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 1.4rem;
    padding-top: 1.1rem;
    border-top: 1px solid var(--stone);
  }

  .sources-label {
    font-size: 0.7rem;
    color: var(--burgundy);
  }

  .source-chip {
    display: inline-flex;
    align-items: baseline;
    gap: 0.5rem;
    padding: 0.45rem 0.9rem;
    border: 1px solid var(--stone);
    border-radius: 6px;
    background: var(--parchment-veil);
    font-size: 0.95rem;
  }

  .source-chip strong {
    font-weight: 600;
  }

  .source-chip em {
    font-size: 0.85rem;
  }

  .foot {
    font-size: 0.68rem;
    text-align: center;
    margin-top: 1rem;
  }

  @media (max-width: 980px) {
    .cards {
      grid-template-columns: 1fr;
      max-width: 30rem;
    }
  }

  /* desktop snap mode on short screens */
  @media (min-width: 1024px) and (hover: hover) and (max-height: 860px) {
    .intro {
      font-size: 1.05rem;
      margin-bottom: 1.1rem;
    }

    .card {
      padding: 1rem 1.1rem 0.9rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    html.js [data-observe] .fill {
      transform: none;
      transition: none;
    }
  }
</style>
